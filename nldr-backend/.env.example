DATABASE_URL=()

# Example Supabase environment variables (do NOT put real service_role keys here)
NEXT_PUBLIC_SUPABASE_URL="https://nolewotwsnrwldgfdadi.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="<ANON_KEY_PLACEHOLDER>"
SUPABASE_SERVICE_ROLE_KEY="<SERVICE_ROLE_KEY_PLACEHOLDER>"

# Optional: restrict the CORS origin the backend accepts requests from.
# Defaults to '*' (any origin) if unset — fine for local dev, but set this
# to your deployed frontend's exact origin in production.
# CORS_ALLOWED_ORIGIN="https://your-frontend.example.com"
